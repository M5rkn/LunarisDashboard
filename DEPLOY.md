# 🚀 Деплой на Railway

## Быстрый старт

### 1. Подготовка
Убедитесь, что у вас есть:
- Аккаунт на [Railway](https://railway.app/)
- Установленный Git
- Проект на GitHub/GitLab

### 2. Деплой через GitHub (рекомендуется)

1. **Запушьте проект в GitHub:**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   git branch -M main
   git remote add origin YOUR_REPO_URL
   git push -u origin main
   ```

2. **Подключите Railway:**
   - Зайдите на [railway.app](https://railway.app/)
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте переменные окружения:**
   - В проекте Railway перейдите в "Variables"
   - Добавьте переменные для Firebase (если нужно)

4. **Готово!**
   - Railway автоматически соберёт и развернёт проект
   - Вы получите URL вида: `your-project.up.railway.app`

### 3. Деплой через Railway CLI

```bash
# Установите CLI
npm i -g @railway/cli

# Войдите в аккаунт
railway login

# Инициализируйте проект
railway init

# Задеплойте
railway up
```

## Настройка Firebase

Если используете Firebase, добавьте переменные окружения в Railway:

1. В проекте Railway → "Variables"
2. Добавьте:
   ```
   VITE_FIREBASE_API_KEY=your_api_key
   VITE_FIREBASE_AUTH_DOMAIN=your_auth_domain
   VITE_FIREBASE_PROJECT_ID=your_project_id
   VITE_FIREBASE_STORAGE_BUCKET=your_storage_bucket
   VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
   VITE_FIREBASE_APP_ID=your_app_id
   ```

## Настройка домена

1. В проекте Railway перейдите в "Settings"
2. В разделе "Domains" нажмите "Add Custom Domain"
3. Введите ваш домен
4. Настройте DNS записи у регистратора домена

## Монетизация

Railway предоставляет:
- $5 кредитов ежемесячно бесплатно
- 500 часов выполнения
- 1GB памяти

## Полезные команды

```bash
# Просмотр логов
railway logs

# Открыть проект в браузере
railway open

# Просмотреть переменные
railway variables

# Удалить проект
railway project delete
```

## Troubleshooting

### Сборка не удаётсяся
- Проверьте `package.json` на наличие всех зависимостей
- Убедитесь, что `npm run build` работает локально

### Firebase не работает
- Проверьте правила безопасности Firestore
- Убедитесь, что переменные окружения настроены правильно

### 404 при навигации
- Убедитесь, что `nginx.conf` копируется в Docker-образ
- Проверьте, что `try_files` настроен правильно

## Ссылки

- [Документация Railway](https://docs.railway.app/)
- [Railway CLI](https://docs.railway.app/cli)
- [Примеры проектов](https://github.com/railwayapp-starters)
