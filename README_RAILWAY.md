# 🚀 Инструкция по деплою на Railway

## Шаг 1: Запушьте проект на GitHub

Откройте PowerShell в папке проекта и выполните:

```powershell
cd C:\Users\Марк\Desktop\dashboard

# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit - Dashboard Analytics"

# Создание основной ветки
git branch -M main
```

Затем создайте репозиторий на GitHub и выполните:

```powershell
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

## Шаг 2: Подключите Railway

1. Зайдите на [railway.app](https://railway.app/)
2. Войдите через GitHub
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Найдите ваш репозиторий и выберите его

## Шаг 3: Настройте проект

Railway автоматически определит Dockerfile и настроит сборку.

1. Перейдите в **"Settings"** вашего проекта
2. Убедитесь, что:
   - **Build Type**: Dockerfile
   - **Root Directory**: (пусто)
   - **Start Command**: `nginx -g 'daemon off;'`

## Шаг 4: Задеплойте

1. Перейдите в **"Deployments"**
2. Нажмите **"Deploy"**
3. Дождитесь завершения сборки (2-5 минут)

## Шаг 5: Проверьте работу

1. После деплоя нажмите **"Generate Domain"**
2. Откройте ссылку вида `your-project.up.railway.app`
3. Проверьте, что всё работает

## 🔧 Настройка Firebase

Firebase уже настроен в проекте. Если нужно изменить конфигурацию:

1. Откройте `src/config/firebase.js`
2. Обновите значения
3. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Update Firebase config"
   git push
   ```
4. Railway автоматически пересоберёт проект

## 📊 Монетизация

Railway предоставляет:
- ✅ $5 кредитов ежемесячно бесплатно
- ✅ 500 часов выполнения
- ✅ 1GB памяти

Это достаточно для небольшого проекта!

## 🎯 Что дальше?

### Добавление своего домена

1. В Railway перейдите в **"Settings"** → **"Domains"**
2. Нажмите **"Add Custom Domain"**
3. Введите ваш домен
4. Настройте DNS у регистратора:
   ```
   Type: CNAME
   Name: @ или www
   Value: your-project.up.railway.app
   ```

### Автоматический деплой

Теперь при каждом `git push` проект будет автоматически обновляться на Railway!

```bash
# Внесите изменения
git add .
git commit -m "Описание изменений"
git push
```

Через 2-3 минуты изменения будут на сайте.

## ❗ Troubleshooting

### Сборка не удаётсяся
Проверьте логи в Railway → Deployments → View Logs

### Firebase не работает
Проверьте правила безопасности Firestore в Firebase Console

### Сайт показывает 404
Убедитесь, что `nginx.conf` правильно настроен (уже сделано)

## 📞 Помощь

- [Документация Railway](https://docs.railway.app/)
- [Discord сообщество](https://discord.gg/railway)
- [GitHub Issues](https://github.com/railwayapp/railway/issues)
